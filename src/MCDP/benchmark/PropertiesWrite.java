package MCDP.benchmark;

public class PropertiesWrite
{
	//private final Properties configProp = new Properties();
	
	public PropertiesWrite()
	{
		// TODO Auto-generated constructor stub
	}
	
	/*public void setProperty(String key, String value)
	{
		  configProp.setProperty(key, value);
	}*/
/*
	public void savePropertiesFile(String filename, Hashtable<String,String> hashTable)
	{
	    try
	    {
	        Properties props = new Properties();
	        props.setProperty("z", hashTable.get("z"));
	        props.setProperty("Y[][]", hashTable.get("Y"));
	        props.setProperty("Z[][]", hashTable.get("Z"));
	        File f = new File("server.properties");
	        OutputStream out = new FileOutputStream( f );
	        props.store(out, "This is an optional header comment string");
	    }
	    catch (Exception e ) {
	        e.printStackTrace();
	    }
		
	}*/
}
